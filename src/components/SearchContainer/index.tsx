import "./styles.scss"

export const SearchContainer = () => {
  return(
    <section  className="container">
      <h1>Book Finder</h1>
      <input type="text" />
      <button>Pesquisar</button>
    </section>
  )
}